# Pin to the runtime used by the target Databricks Runtime (this project targets DBR 15.4 LTS)
pyspark==3.5.0
delta-spark==3.2.0
pyyaml==6.0.1

# Dev / test / lint only -- not needed on the cluster (Databricks provides pyspark/delta there)
pytest==8.2.2
ruff==0.5.0
